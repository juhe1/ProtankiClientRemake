package alternativa.tanks.battle.objects.tank.controllers
{
   [ModelInterface]
   public interface LocalTurretControllerFactory
   {
      
      function createLocalTurretController(param1:TurretController) : LocalTurretController;
   }
}

